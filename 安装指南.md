# 觉心师父回复助手 · Android App

## 🎯 功能

| 功能 | 说明 |
|------|------|
| 🔴 悬浮球 | 始终在微信等任何App上层，可拖拽 |
| 📋 剪贴板监听 | 在微信复制弟子消息后，自动弹出回复面板 |
| 🤖 智能生成 | 7大场景模板 + 纯本地引擎，无需网络 |
| ✏️ 一键复制 | 选一条回复自动复制，回微信粘贴即发送 |
| 🔕 零打扰 | 悬浮球半透明、通知静默、不抢焦点 |

## 📲 两种构建方式

### 方式一：Android Studio（推荐，最简单）

1. 下载 [Android Studio](https://developer.android.com/studio)
2. 打开此目录 `juexin-android-app/`
3. 等待Gradle同步完成（首次约5-10分钟）
4. USB连接安卓手机 → 开启开发者选项 → USB调试
5. 点击工具栏 ▶ Run → 选择你的手机
6. APK自动安装到手机

### 方式二：GitHub Actions（无需本地环境）

1. 将此项目上传到你的GitHub仓库
2. 进入 Actions → Build Android APK → Run workflow
3. 等待构建完成（约5分钟）
4. 下载 `juexin-assistant-debug.apk`
5. 传到手机安装

## 📱 安装后操作

### 第一步：授权
打开「觉心助手」→ 点击「授权悬浮窗权限」→ 找到觉心助手 → 开启

### 第二步：启动
点击「启动助手」→ 通知栏出现「悬浮球已就绪」→ 屏幕右侧出现悬浮球

### 第三步：使用
```
微信中长按弟子消息 → 复制
       ↓
悬浮球自动弹出回复面板（或点击悬浮球）
       ↓
┌─────────────────────┐
│ 🤲 慈悲共情  [编辑] │
│ 阿弥陀佛。弟子...    │
├─────────────────────┤
│ 🔮 因果开示  [编辑] │
│ 弟子，师父观你...    │
├─────────────────────┤
│ ✨ 行动指引  [编辑] │
│ 弟子，先把...        │
└─────────────────────┘
       ↓
点任意一条 → 自动复制
       ↓
切回微信 → 长按输入框 → 粘贴 → 发送
```

## 🔧 技术架构

- 语言：Kotlin
- 最低版本：Android 8.0 (API 26)
- 大小：约3MB
- 权限需求：
  - 悬浮窗权限（显示悬浮球和回复面板）
  - 前台服务（保持后台运行）
  - 网络权限（仅用于未来AI API扩展，当前本地引擎不需要）

## 📂 项目结构

```
juexin-android-app/
├── app/src/main/
│   ├── java/com/juexin/assistant/
│   │   ├── MainActivity.kt          # 权限引导页
│   │   ├── FloatingBallService.kt    # 悬浮球+回复面板
│   │   ├── ClipboardService.kt       # 剪贴板监听
│   │   └── ReplyGenerator.kt         # 回复生成引擎（7场景）
│   ├── res/
│   │   ├── layout/activity_main.xml  # 主界面
│   │   ├── layout/panel_reply.xml    # 回复面板
│   │   ├── drawable/                 # 图标资源
│   │   └── values/                   # 主题配色
│   └── AndroidManifest.xml
├── build.gradle.kts
├── settings.gradle.kts
└── .github/workflows/build-apk.yml   # 自动构建
```

## ⚠️ 注意事项

- 首次安装后需手动授权悬浮窗权限（Android安全要求）
- 悬浮球半透明，不会遮挡微信内容
- 完全本地运行，不收集任何用户数据
- 华为/小米等手机可能需额外关闭「锁屏清理」「省电优化」
