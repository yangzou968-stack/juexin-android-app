# 推送项目到GitHub并自动构建APK

## 前提准备

### 1. 安装Git（如未安装）
打开终端，输入：
```bash
xcode-select --install
```
在弹出的窗口中点「安装」。完成后 `git --version` 应能正常显示。

### 2. 网络确认
确保能访问 https://github.com ，如不行需要开VPN。

---

## 方式一：一键脚本（推荐）

```bash
# 1. 先在浏览器打开 https://github.com/new 创建一个空仓库
#    - Repository name: juexin-android-app
#    - 选 Private 或 Public
#    - 不要勾选初始化（README/LICENSE等都不选）
#    - 创建后复制仓库URL，如: https://github.com/你的名/juexin-android-app.git

# 2. 在终端运行
cd /Users/qw/Desktop/佛学/juexin-android-app
bash push-to-github.sh https://github.com/你的名/juexin-android-app.git

# 3. 输入GitHub用户名和密码（或Token）
```

---

## 方式二：手动上传（无需Git）

1. 在Finder中打开 `/Users/qw/Desktop/佛学/juexin-android-app`
2. 全选所有文件 → 右键 → 压缩（得到 `归档.zip`）
3. 浏览器打开 https://github.com/new 创建仓库
4. 在仓库页面点击「uploading an existing file」
5. 拖入 zip 文件 → Commit changes

> ⚠️ 手动上传不会触发Actions。需要在仓库页面点 Actions → Build Android APK → Run workflow 手动触发。

---

## 构建完成后下载APK

1. 打开仓库页面 → 点顶部 **Actions** 标签
2. 点最新的 workflow run（名称: "Build Android APK"）
3. 等黄色圆点变绿色勾 ✅
4. 页面底部 **Artifacts** 区域 → 下载 `juexin-assistant-debug`
5. 解压 zip → 得到 `app-debug.apk`
6. 传到手机安装

---

## 安装到手机后

1. 打开App → 授予「悬浮窗权限」
2. 点「启动助手」
3. 微信中复制弟子消息 → 悬浮球自动弹出回复 → 点一条复制 → 粘贴发送

---

## 常见问题

**Q: 安装时提示"未知来源"？**
A: 设置 → 安全 → 允许安装未知应用 → 授权给浏览器/文件管理器

**Q: 悬浮球不显示？**
A: 设置 → 应用 → 觉心助手 → 权限 → 打开「悬浮窗」权限

**Q: 复制消息后没反应？**
A: 确保悬浮球显示为红点状态（不在微信界面时打开助手检查）

**Q: Actions构建失败了？**
A: 检查 build-apk.yml 日志，多数情况是Gradle版本或依赖问题。将Actions日志发给我诊断。
